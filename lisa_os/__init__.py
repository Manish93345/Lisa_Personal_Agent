"""
LOSA — Lisa Operating System Architecture (v3)

This is the new, LangGraph-native package. It grows phase by phase
(see 08_ROADMAP.md / Blueprint Section 14). Nothing in the old
`actions.zip` codebase is deleted — modules here progressively wrap
and then replace the old code, per the Refactor Map (Blueprint
Section 13).

Phase 0 (now):  this skeleton only. No logic yet.
Phase 1:        graph.py, state.py, schemas.py, nodes/ get real code.
"""
